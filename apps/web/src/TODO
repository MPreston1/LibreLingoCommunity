transform static/images into git-lfs
Understand how to transform Hero.svelte into index.svelte

Upgrade bulma theme with the bulmaswatch one
Upgrade import to @use

Enable auth user (NavBar.svelte)

Remove warnings mute in vite.config.ts

import live e getSkillStats commented, restore.

Test courses with gists

Change vite md plugin for parsing directly html